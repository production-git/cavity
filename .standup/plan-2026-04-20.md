# Standup Plan — 2026-04-20

## Yesterday
- Shipped Phase A1 scaffold: Three.js r160 via import map, `renderer-three.js` (atoms-as-spheres with PerspectiveCamera + lights), `?renderer=three` feature flag, 4 new unit tests (total suite 169 green)
- Nothing in-progress — all 4 planned A1 tasks closed

## Today's Goal
Extend the Three.js renderer to draw bonds, keeping the Canvas 2D path untouched and the test gate green.

## Tasks
- [ ] T1: Render bonds as `CylinderGeometry` in `renderer-three.js` (oriented between atom positions, radius ~0.1)
- [ ] T2: Use a reusable `bondGroup` alongside `atomGroup`; clear + rebuild on each `draw()` for now (match atom pattern)
- [ ] T3: Handle dashed bonds — simplest acceptable: render as thinner/darker cylinder (mark TODO for proper dashing)
- [ ] T4: Add 2 unit tests — bond mesh count matches `app.bonds.length`; `draw()` no-throw with zero bonds
- [ ] T5: Update `Docs/progress.md` with A1.5 entry; bump planned-features.md A1 note ("bonds shipped, polyhedra/picking/fog pending")

## Files to touch
- `app/renderer-three.js`                    <!-- ~1k, 47 lines — add bondGroup + cylinder loop -->
- `app/tests/unit/renderer-three.test.js`    <!-- ~2k, 73 lines — add 2 tests, extend three mock -->
- `Docs/progress.md`                         <!-- ~8k — append A1.5 entry -->
- `Docs/feature_plans/planned-features.md`   <!-- ~3k — update A1 status note -->

## Read-only reference (do NOT edit)
- `app/renderer.js` (bond drawing logic, lines handling `bond.dashed`) — for parity reference only
- `app/state.js` (shape of `app.bonds` entries: `{a, b, dashed}`, and `app.atoms[i]` lookup) — confirm data model

## Context budget
- Edit targets: 4 files ≈ ~14k tokens
- Read-only reference: 2 files (`renderer.js` ~15k, `state.js` ~20k) — open only the specific sections needed, not whole files
- Plan + system overhead: ~13k tokens
- Total estimated load: ~45–60k / 200k tokens
- Headroom for Sonnet reasoning + codegen: ~140k+ tokens
- ⚠ Large files: `state.js` 796 lines (~20k), `renderer.js` 597 lines (~15k) — scope reads narrowly
- ✓ Safe to skip today: `app/ui.js`, `app/index.html`, `app/math3d.js`, `app/index.js`, all integration/e2e tests, all phase-2 docs

## Architectural notes
- Canvas 2D `renderer.js` is deprecated long-term (Phase A) but MUST stay working as default until A1+A2+A3 complete — do not refactor it
- Mesh rebuilding every frame is intentional for now; `Object3D` caching / dirty-flag comes later once polyhedra land and frame budget matters
- `renderer-three.js` is still ungated by picking/stats/legend wiring — those belong to A2, not today
- Bond geometry: use the simplest cylinder-between-two-points approach (midpoint + `lookAt` or quaternion from `(0,1,0)` → bond direction). Avoid `TubeGeometry` — overkill for straight bonds.

## Deferred
- Polyhedra rendering (A1 day 3): pushed because bonds alone is a clean, shippable slice
- Proper dashed-bond rendering (shader or `Line2` dashed material): TODO'd; today uses thin/darker cylinder stand-in
- Picking / raycaster wiring to `ui.js` (A2): outside today's scope
- BVH hit testing (A2), GC-free loop (A2), glassmorphism UI (A3): parallel Phase A tracks, not today
