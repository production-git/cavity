# Standup Report — 2026-04-28

---

## 🧑 What changed for you today

### New
- **Cavity spheres in WebGL renderer**: When using the experimental Three.js renderer (`?renderer=three`), cavity spheres now appear as translucent golden spheres at their correct positions and sizes — matching the visual you already see in the default Canvas 2D view.

### Not visible yet
- The default Canvas 2D renderer is unchanged and fully functional.
- Picking (clicking atoms/bonds to select them), depth fog, and proper dashed bonds are still pending in the Three.js renderer.

---

## 🤖 Technical summary (for tomorrow's Opus planning session)

### Completed
- **A1.7**: Cavity sphere rendering — `isSphere:true` draw groups now rendered as `SphereGeometry(r, 16, 16)` + `MeshBasicMaterial` (opacity 0.3, depthWrite:false) centred at `(cx, cy, cz)` inside `polyGroup`. 3 new unit tests added. (`app/renderer-three.js`, `app/tests/unit/renderer-three.test.js`)
- **Docs**: `Docs/progress.md` A1.7 section appended; `planned-features.md` A1 note updated (cavity spheres shipped, picking/fog/dashed bonds pending).

### In Progress
- Nothing.

### Blocked
- Nothing.

### Scope discoveries
- No plan file existed for today (2026-04-28) — planning routine must have been skipped. Implemented based on yesterday's report notes which clearly stated "A1.7 next."
- No unlisted files needed; plan from 2026-04-21 report was accurate.

### Notes for tomorrow
- **A1 is now feature-complete for geometry rendering**: atoms, bonds, polyhedra, cavity spheres all ship in `?renderer=three`.
- **A2 next** (picking/raycaster) or **A1.8** (depth fog parity) — suggest A1.8 as smaller scope: port `app.depthFog` toggle and distance-based opacity to renderer-three.js.
- Alternatively, could consider the Three.js renderer "good enough for geometry" and pivot to A2 BVH + picking — higher impact for interactivity.
- `renderer-three.js` is 143 lines — well within the 800-line file cap.

### Test status
- Suite: 177 passing, 0 failing
- Coverage: 88.25% statements, 79.43% branch, 92.85% functions
- E2E: skipped (no server-side changes)
