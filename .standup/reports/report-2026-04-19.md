# Standup Report — 2026-04-19

## Completed
- **T1:** Added Three.js r160 CDN import map to `app/index.html` (`<script type="importmap">` with jsdelivr URL); installed `three@0.160.0` as devDependency so Jest can resolve it for tests
- **T2:** Created `app/renderer-three.js` — exports `init(canvas)` and `draw()`; scene uses PerspectiveCamera (45°, z=40), ambient + directional lights; atoms rendered as `SphereGeometry` meshes with element colour/radius; rotation synced to `app.angleX/Y`
- **T3:** Added `?renderer=three` feature flag in `app/index.js` — uses top-level `await import('./renderer-three.js')` on the Three.js path; Canvas 2D path (`renderer.js`) unchanged as default
- **T4:** Added 4 unit tests in `app/tests/unit/renderer-three.test.js` using `jest.unstable_mockModule` for ESM mocking of `three` and `state.js`; all pass

## In Progress
- None — all 4 planned tasks shipped

## Blocked
- None

## Notes for tomorrow
- Next step: bonds as `TubeGeometry` / `CylinderGeometry` in renderer-three.js (A1 day 2)
- `ui.js` still calls Canvas 2D `draw` directly — the Three.js path won't update stats/legend/selection on user actions until A2 wires picking through the raycaster
- Mesh rebuilding on every `draw()` call is intentional for now (simple); consider `dirty` flag or `Object3D` caching once bonds are added and frame budget matters

## Test status
- Suite: 169 tests passing, 0 failing
- Coverage: 88.25% statements, 79.43% branch, 92.85% functions (gate: 80% / 80% / 80%)
- E2E: skipped (Playwright not run today; Canvas 2D path unchanged)
