# Standup Report — 2026-04-21

---

## 🧑 What changed for you today

### New
- **Polyhedra in WebGL renderer**: When you switch to the experimental Three.js renderer (`?renderer=three`), the coloured polyhedra (copper-oxygen planes, carbon rings, etc.) now appear as translucent face meshes with solid edges — matching what you already see in the default Canvas 2D view.

### Not visible yet
- Cavity spheres in the Three.js renderer are still missing (that's the next slice, A1.7). The default Canvas 2D renderer is unchanged and fully functional.

---

## 🤖 Technical summary (for tomorrow's Opus planning session)

### Completed
- **T1**: `polyGroup` added to `renderer-three.js` — declared, initialised in `init()`, cleared/rebuilt on every `draw()`, rotation synced with `atomGroup`/`bondGroup`.
- **T2**: Face mesh builder — fan triangulation from `group.faces[]` (each face is `[id0, id1, ...]`), `BufferGeometry` + `BufferAttribute` position array, `MeshBasicMaterial` (opacity 0.25, DoubleSide, depthWrite:false). Skips triangles with missing atom IDs gracefully.
- **T3**: Edge lines — `LineSegments` per group using `BufferGeometry.setFromPoints` from `group.edges[]`, `LineBasicMaterial` with group color.
- **T4**: 3 new unit tests added — no-throw with zero polyhedra; `BufferGeometry` count = 2 × non-sphere groups (1 face geo + 1 edge geo each); sphere groups skipped (no `MeshBasicMaterial`). THREE mock extended with `BufferGeometry`, `BufferAttribute`, `MeshBasicMaterial`, `LineBasicMaterial`, `LineSegments`, `DoubleSide`.
- **T5**: `Docs/progress.md` A1.6 section appended; `planned-features.md` A1 note updated (polyhedra shipped, cavity spheres/picking/fog pending).

### In Progress
- Nothing.

### Blocked
- Nothing.

### Scope discoveries
- None. Plan was accurate; all 4 files touched, no unlisted files needed.

### Notes for tomorrow
- **A1.7 next**: cavity sphere rendering in Three.js — `isSphere:true` groups need `SphereGeometry` meshes centered at `(cx, cy, cz)` with radius `r`. Two rings for orientation are a Canvas 2D artefact; Three.js should just render the sphere mesh directly with low-opacity material.
- `getAllDrawGroups()` import added to `renderer-three.js` — this was the only dependency change; no circular imports introduced (direction still math3d ← state ← renderer-three ← index).
- Fan triangulation safe because `convexHull3DFaces` already returns ordered vertices.

### Test status
- Suite: 174 passing, 0 failing
- Coverage: 88.25% statements, 79.43% branch, 92.85% functions
- E2E: skipped (not run today — no server-side changes)
